apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: one-test

bases:
- ../../../plans/paid

commonLabels:
  app.kubernetes.io/instance: tenant-one
  example.com/stage: test-one

resources:
- namespace.yaml
